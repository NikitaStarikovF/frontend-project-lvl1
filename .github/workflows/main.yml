name:  Ml

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
          - name: git clone my repo
          - uses: actions/checkout@v1
          
          - name: Make lint
          - run:  make lint
            
            
            


