name:  Ml

on:
  push:
    branches:
     -master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
          - name: Git clone my repo
          - uses: actions/checkout@v1
          
          - name: Make lint
            run:  make lint
            
            
            


