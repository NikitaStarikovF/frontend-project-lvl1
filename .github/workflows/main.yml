name:  Ml

on:
  push:
    branches:
     -master
jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]
        
  
    steps:
          - name: Git clone my repo
            uses: actions/checkout@v1
          
          - name: Make lint
            run : make lint
            
            
            


